# Screenshot Gallery

Every screenshot below is generated by CI from configs defined in
[`scripts/screenshots/configs.lua`](scripts/screenshots/configs.lua).
Each screenshot enables only **one** config option, so you can see exactly
which strings it highlights (and which it doesn't).

If something looks off, click the indexed `[N]` link next to any screenshot
to open a pre-filled issue, or
[open an issue](https://github.com/catgoose/nvim-colorizer.lua/issues)
and reference the config key.

## Table of Contents

- [Default](#default)
- [Hex Colors](#hex-colors)
- [CSS Functions](#css-functions)
- [Named Colors](#named-colors)
- [Special Parsers](#special-parsers)

## Default

Mixed showcase with `css = true` preset — enables names, hex, rgb, hsl, oklch all at once.

<!-- gen:default_gallery:start -->
<!-- gen:default_gallery:end -->

## Hex Colors

Each screenshot uses its own fixture with only the matching format in "should highlight".
The `hex_all` screenshot uses [`hex_all.css`](scripts/screenshots/fixtures/hex_all.css) to show all hex formats together.

<!-- gen:hex_gallery:start -->
<!-- gen:hex_gallery:end -->

## CSS Functions

Each screenshot uses its own fixture with only the matching function in "should highlight".
The `css_all` screenshot uses [`css_all.scss`](scripts/screenshots/fixtures/css_all.scss) to show all CSS functions together.

<!-- gen:css_gallery:start -->
<!-- gen:css_gallery:end -->

## Named Colors

Each screenshot uses its own fixture with only the matching name style in "should highlight".
The `names_all` screenshot uses [`names_all.css`](scripts/screenshots/fixtures/names_all.css) to show all name styles together.

<!-- gen:names_gallery:start -->
<!-- gen:names_gallery:end -->

## Special Parsers

Each special parser uses its own fixture with an appropriate file type:
[`xterm.sh`](scripts/screenshots/fixtures/xterm.sh),
[`xcolor.tex`](scripts/screenshots/fixtures/xcolor.tex),
[`css_var_rgb.css`](scripts/screenshots/fixtures/css_var_rgb.css).

<!-- gen:special_gallery:start -->
<!-- gen:special_gallery:end -->
